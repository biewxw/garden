# Arcticles

The idea of ​​this folder is to create a file for each article that I am reading or finished reading, so it serves as a reference center for other notes that use that article at some point.